# Runtime dependencies — keep in sync with pyproject.toml [project.dependencies]
# Generate with: pip-compile pyproject.toml (or update manually)
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.7.0
